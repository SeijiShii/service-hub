# バグ調査レポート: DB スキーマ未適用 + push 機構なし

> **issue / slug**: 001 / schema-push
> **重大度**: high（機能ブロッカー、本番影響ゼロ＝デプロイ前検出）
> **再現可否**: 可
> **報告日**: 2026-05-26
> **対象**: ../README.md（_shared/db）

## 1. 症状
認可が通っても、ダッシュボード/詳細 API が DB クエリで `relation does not exist` で失敗する見込み。Neon の `public` スキーマにテーブルが 1 つも無い。

## 2. 期待挙動
`src/db/schema.ts` の 3 テーブル（`usage_snapshots` / `alert_events` / `collection_runs`）が Neon に存在し、`db:push` 等でスキーマを適用できる。

## 3. 再現手順
1. `select tablename from pg_tables where schemaname='public'` → 0 件（確認済）。
2. `drizzle.config` 不在、`package.json` に push/migrate script 不在 → 適用手段が無い。

## 4. 再現環境
| 項目 | 値 |
|---|---|
| ORM | drizzle-orm 0.45 / drizzle-kit 0.31（依存はあるが未設定） |
| DB | Neon Postgres（pooled、検証済） |

## 5. 影響範囲
- 影響機能: DB に依存する全 API（dashboard / service-detail / cron collect）。
- データ影響: なし　/　**本番影響: なし（デプロイ前検出）**

## 6. 暫定回避策
手動 DDL 投入（非推奨。schema.ts と乖離する）。

## 7. 関連ログ
```
ls drizzle.config.* → 不在
package.json scripts → db:push / migrate 無し
Neon public tables → (なし)
```

## 8. 過去類似バグ
なし。

## 9. 関連 AI_LOG タイムライン
- D20260526_008（反復2 db）: pglite で offline 統合テスト。**実 Neon への適用機構は未整備**のまま。
- D20260526_010（release Phase2, D051）: 本ギャップ検出。

## 10. 次のステップ
→ 001_ROOT_CAUSE
