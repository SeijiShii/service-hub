# dashboard E2E テスト計画（admin 導線 + admin スタイリング）

> **入力**: 001/002, 既存ダッシュボード / admin E2E
> **最終更新**: 2026-05-28

---

## 1. 変更 UC シナリオ

### UC-AL1: dashboard → admin 導線
| シナリオ ID | 前提 | 操作 | 期待 |
|---|---|---|---|
| E-AL-01 | seiji 認証済 | `/` を開く → ヘッダの「管理」リンクをクリック | `/admin` に遷移、フォームが表示される |

### UC-AS1: admin フォーム視認
| E-AS-01 | seiji 認証済 / `/admin` 表示 | 視覚チェック | 入力欄が縦に整列、ラベル上配置、3 セクションが視認可、登録ボタンが強調されている |
| E-AS-02 | 同上 | フォーム送信 | 既存機能どおり登録できる（styling は機能に影響しない） |

## 2. リグレッションシナリオ
| UC | シナリオ ID | 確認観点 |
|---|---|---|
| ダッシュボード既存表示 | R-DB-01 | サービス一覧・最新 snapshot 表示が不変 |
| admin 機能 | R-AD-01 | 登録/編集/退役 が引き続き動作（styling のみ変更） |
| /admin force-pull ボタン | R-AD-02 | 別 revise の「今すぐ pull」ボタンも引き続き表示・動作（実装後） |

## 3. 移行検証シナリオ
なし。

## 4. 環境要件差分
なし。

## 5. 期待 KPI
- 直 URL でしか到達不可だった `/admin` がダッシュボードから 1 クリックで到達可能（O55 orphaned page 解消）
- `/admin` フォームが目視で使える状態（スクショの素 HTML 状態 → スタイル付きへ）

## 6. 更新履歴
| 日付 | 変更概要 | 実行者 |
|---|---|---|
| 2026-05-28 | 初版作成 | /flow:revise |
