---
generated_at: 2026-05-26 09:06
generator: /flow:estimate
context: whole
source_input: ./docs/concept.md + docs/_shared/types/(001-003)
phase: refined
confidence_band:
  ai_impl: "±100%"
  human_bottleneck: "±50%"
  ai_tokens: "±30% (設計) / ±50% (実装込み)"
calibration:
  data_point: "_shared/types 設計実績 (6 src files / ~210 LOC)"
  delta_vs_rough: "ファイル数 約 +60% (rough は粒度を過小)、LOC は概ね一致"
  global_metrics: "empty → PJ 内 1 データ点のみで補正"
nfr_profile: { scale: low, throughput: low, latency: standard, availability: none, source: explicit }
summary:
  min:  { total_files: 50,  total_lines: 2900, human_hours: 4,  ai_tokens_total: "600K" }
  standard: { total_files: 120, total_lines: 7200, human_hours: 8,  ai_tokens_total: "1.5M" }
  full: { total_files: 230, total_lines: 14500, human_hours: 16, ai_tokens_total: "3.0M" }
---

# 見積もり: service-hub（全体・refined / 2 回目）

> SCENARIO §3 Phase 2 最初の 1 feature (`_shared/types`) 完了直後の **2 回目（refined）**。
> rough（`全体_20260526_service-hub.md`、±300%）からの再校正版。**band は ±300% → ±100% に収束**。

## 再校正の根拠（calibration）

| 観点 | rough 想定 | _shared/types 実設計 | 補正 |
|---|---|---|---|
| 1 横断フォルダのファイル数 | Min 1 / Std 2 | 6 src files (service/provider/metric/alert/index/guards) | **ファイル数を約 +60% 上方修正** |
| 1 横断フォルダの LOC | Std ~200 | ~210 (logic 180 + test 30) | 概ね一致（据え置き） |
| 設計トークン/feature | ~10K | ~9K（types は小、E2E スキップ） | 据え置き |

→ **LOC・トークンは rough がほぼ妥当、ファイル数のみ上方修正**。band を ±100% に収束（実装メトリクス蓄積前のため ±50% までは詰めない）。

## サマリ表

| スコープ | ファイル数 | コード行数 (logic+test) | 人間時間 | AI 推論トークン (設計+実装) |
|---|---|---|---|---|
| Minimum | 50 | 2,900 | 4h | 設計 60K / 実装 540K = **600K** (±50%) |
| **Standard** | **120** | **7,200** | **8h** | 設計 135K / 実装 1.37M = **1.5M** (±50%) |
| Full | 230 | 14,500 | 16h | 設計 270K / 実装 2.73M = **3.0M** (±50%) |

> **confidence band**: AI-impl **±100%**（refined、1 feature 設計済）/ Human ±50% / トークン ±30%設計・±50%実装
> **NFR**: scale=low/throughput=low/latency=standard/availability=none（低スケール前提は baseline 織込済）
> 残る最大不確実性は **`_shared/providers`**（[論点-001] 各 PaaS 使用量 API の実在性）。これが詰まると次回（providers 設計後）さらに収束。

## 主な変更点（rough → refined）

1. **ファイル数 +約 15 件**（Std 85→120）: 各フォルダの実ファイル粒度を `_shared/types` 実績で上方修正（型/ガード/バレル分割等で 1 フォルダ 4-6 ファイルが標準と判明）。
2. **LOC ほぼ据え置き**（Std 7,000→7,200）: 行数見積は妥当だった。
3. **band 収束**（±300%→±100%）: 設計が 1 つ確定し、構造の不確実性が減少。
4. **トークン据え置き**（Std ~1.5M）: 設計 9 フォルダ + 実装 tdd 12 セッション規模は変わらず。

## 次の収束ポイント
- `_shared/providers` 設計後（[論点-001] 解決）に再 invoke すると、最大不確実性が解消し band がさらに縮む。
- 実装（tdd）開始後は実 LOC/tokens/active_min が global-metrics に蓄積され、`/flow:status` で精度確認可能。
