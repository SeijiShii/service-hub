# collection E2E テスト計画（ダッシュボード最終更新表示）

> **入力**: 001/002, 既存ダッシュボード E2E
> **最終更新**: 2026-05-28（スコープ縮小: cron 移行撤回）

---

## 1. 変更 UC シナリオ

### UC-LU1: ダッシュボード最終更新表示
| シナリオ ID | 前提 | 操作ステップ | 期待 |
|---|---|---|---|
| E-LU-01 | run 履歴あり | `/` を開く | ヘッダに「最終更新: YYYY-MM-DD HH:MM (xx 分前)」表示 |
| E-LU-02 | run 履歴なし | `/` を開く | 「未収集」表示 |
| E-LU-03 | 最新 run が failed | `/` を開く | 警告色 + status 表示 |
| E-LU-04 | 手動補完シナリオ | E-LU-01 状態 → /admin で force-pull → `/` を再表示 | 最終更新が直近時刻に更新（force-pull revise と組み合わせて確認） |

## 2. リグレッションシナリオ
| UC | シナリオ ID | 確認観点 |
|---|---|---|
| `/api/cron/collect` 経路 | R-CR-01 | Vercel Cron 日次が引き続き動作（**スコープ撤回**でこの経路は無変更） |
| ダッシュボード既存表示 | R-DB-01 | サービス一覧・最新 snapshot 表示が不変 |

## 3. 移行検証シナリオ
なし。

## 4. 環境要件差分
| 項目 | 前回 | 今回 |
|---|---|---|
| 自動 pull の実行元 | Vercel Cron 日次 | **無変更**（Vercel Cron 日次のまま） |
| 鮮度が要る時の補完 | 手動 curl | **`/admin` の force-pull ボタン**（別 revise） |

## 5. 期待 KPI
- ダッシュボードに「最終更新」が常時表示（達成目標）

## 6. 更新履歴
| 日付 | 変更概要 | 実行者 |
|---|---|---|
| 2026-05-28 | 初版作成 | /flow:revise |
| 2026-05-28 | スコープ縮小（cron 関連シナリオ撤去、最終更新 UC のみ + force-pull 連携を E-LU-04 として追加） | /flow:revise (scope amend) |
