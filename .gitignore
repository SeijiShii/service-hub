# secrets (perspectives O25 — 各 PaaS トークンが集中するため厳守)
.env
.env.local
.env*.local

# deps / build
node_modules/
dist/
.vercel/
.turbo/

# exports / logs
exports/
*.log

# flow 一時マーカー (continuous loop / compact 連携、追跡しない)
.flow-loop-active
.flow-needs-compact

# OS
.DS_Store

# playwright
test-results/
playwright-report/
.vercel
